const express=require('express');const path=require('path');const{Client}=require('pg');const{nanoid}=require('nanoid');const app=express();const PORT=process.env.PORT||8080;const db=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});async function initDB(){await db.connect();await db.query('CREATE TABLE IF NOT EXISTS patients(id VARCHAR(20) PRIMARY KEY,data JSONB NOT NULL,created_at TIMESTAMP DEFAULT NOW())');console.log('DB ready');}app.use(express.json({limit:'5mb'}));app.use(express.static(path.join(__dirname,'..','public')));app.post('/api/patients',async(req,res)=>{try{const{data}=req.body;const id=nanoid(8);await db.query('INSERT INTO patients(id,data)VALUES($1,$2)',[id,data]);res.json({id,url:'/p/'+id});}catch(e){res.status(500).json({error:e.message});}});app.get('/api/patients/:id',async(req,res)=>{try{const r=await db.query('SELECT * FROM patients WHERE id=$1',[req.params.id]);if(!r.rows.length)return res.status(404).json({error:'No encontrado'});res.json({id:r.rows[0].id,data:r.rows[0].data});}catch(e){res.status(500).json({error:e.message});}});app.get('/api/patients',async(req,res)=>{try{const r=await db.query('SELECT id,data,created_at FROM patients ORDER BY created_at DESC');res.json(r.rows.map(row=>({id:row.id,name:row.data&&row.data.patient?row.data.patient.name:'Sin nombre',dx:row.data&&row.data.patient?row.data.patient.dx:'',createdAt:row.created_at})));}catch(e){res.status(500).json({error:e.message});}});app.delete('/api/patients/:id',async(req,res)=>{try{await db.query('DELETE FROM patients WHERE id=$1',[req.params.id]);res.json({ok:true});}catch(e){res.status(500).json({error:e.message});}});app.get('/p/:id',(req,res)=>{res.sendFile(path.join(__dirname,'..','public','patient.html'));});app.get('/editor',(req,res)=>{res.sendFile(path.join(__dirname,'..','public','editor.html'));});initDB().then(()=>{app.listen(PORT,()=>console.log('Server running on port '+PORT));}).catch(e=>{console.error('DB error:',e.message);process.exit(1);});
